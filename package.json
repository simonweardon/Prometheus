{
  "name": "prometheus-scheduler",
  "version": "1.0.0",
  "description": "Google Meet auto-scheduler for Prometheus diagnostic calls",
  "main": "scheduler.js",
  "scripts": {
    "start": "node scheduler.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "googleapis": "^131.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
